<nav aria-label="Wizard navigation" id="{{ ctx.wizardKey }}-header" ref="{{ctx.wizardKey}}-header">
  <ul class="flex gap-1" role="tablist">
    {% ctx.panels.forEach(function(panel, index) { %}
    <li class="{{ctx.currentPage === index ? ' active' : ''}}" style="cursor: pointer;">
      <button tabindex="0" data-index="{{index}}" role="tab" class="inline-flex items-center justify-center rounded-md px-3 py-1.5 text-sm font-medium border border-input hover:bg-accent transition-colors{{ctx.currentPage === index ? ' bg-primary text-primary-foreground border-primary hover:bg-primary/90' : ''}}" ref="{{ctx.wizardKey}}-link">
        {{ctx.t(panel.title, { _userInput: true })}}
        {% if (panel.tooltip && ctx.currentPage === index) { %}
        <i ref="{{ctx.wizardKey}}-tooltip" class="{{ctx.iconClass('question-sign')}} text-muted-foreground" data-tooltip="{{panel.tooltip}}"></i>
        {% } %}
      </button>
    </li>
    {% }) %}
  </ul>
</nav>
