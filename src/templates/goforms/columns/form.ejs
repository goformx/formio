<div class="grid grid-cols-12 gap-4">
{% ctx.component.columns.forEach(function(column, index) { %}
<div class="col-span-{{column.currentWidth}}{% if (column.offset) { %} col-start-{{column.offset + 1}}{% } %}"
    ref="{{ctx.columnKey}}">
  {{ctx.columnComponents[index]}}
</div>
{% }) %}
</div>
