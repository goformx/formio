<table class="{{ ctx.cssClasses['form-table'] }}
    {{ ctx.component.striped ? ' ' + cssClasses['form-table-striped'] : ''}}
    {{ ctx.component.bordered ? ' ' + cssClasses['form-table-bordered'] : ''}}
    {{ ctx.component.hover ? ' ' + cssClasses['form-table-hover'] : ''}}
    {{ ctx.component.condensed ? ' ' + cssClasses['form-table-condensed'] : ''}}
  ">
  <caption class="{{ ctx.cssClasses['form-table-caption'] }}">{{ctx.t(ctx.component.label)}}</caption>
  {% if (ctx.component.header && ctx.component.header.length > 0) { %}
  <thead class="{{ ctx.cssClasses['form-table-header'] }}">
    <tr class="{{ ctx.cssClasses['form-table-row'] }}">
      {% ctx.component.header.forEach(function(header) { %}
      <th class="{{ ctx.cssClasses['form-table-header-cell'] }}" scope="col">{{ctx.t(header)}}</th>
      {% }) %}
    </tr>
  </thead>
  {% } %}
  <tbody class="{{ ctx.cssClasses['form-table-body'] }}">
    {% ctx.tableComponents.forEach(function(row, rowIndex) { %}
    <tr class="{{ ctx.cssClasses['form-table-row'] }}" ref="row-{{ctx.id}}">
      {% row.forEach(function(column, colIndex) { %}
      <td class="{{ ctx.cssClasses['form-table-cell'] }}{% if (ctx.cellClassName) { %} {{ctx.cellClassName}}{% } %}" scope="col" ref="{{ctx.tableKey}}-{{rowIndex}}">{{column}}</td>
      {% }) %}
    </tr>
    {% }) %}
  </tbody>
</table>
