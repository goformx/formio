<% const cssClasses = require('../cssClasses'); %>
<div
  class="<%= cssClasses['form-multi-value'] %>"
  ref="{{ctx.input.ref ? ctx.input.ref : 'input'}}"
>
  <select
    class="<%= cssClasses['form-multi-value-select'] %>"
    id="{{ctx.key}}-mask"
    ref="select"
    {% if (ctx.input.attr.disabled) { %}disabled{% } %}>
    {% ctx.selectOptions.forEach(function(option) { %}
    <option value="{{option.value}}">{{option.label}}</option>
    {% }); %}
  </select>
  <input
    ref="mask"
    class="<%= cssClasses['form-multi-value-input'] %>"
    {% for (var attr in ctx.input.attr) { %}
    {{attr}}="{{ctx.input.attr[attr]}}"
    {% } %}
  >
</div>
