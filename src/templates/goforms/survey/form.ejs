<table class="w-full text-sm [&_tbody_tr:nth-child(odd)]:bg-muted/50 border border-border [&_th]:border [&_th]:border-border [&_td]:border [&_td]:border-border" aria-labelledby="l-{{ctx.instance.id}}-{{ctx.component.key}}">
  <thead>
    <tr>
      <th></th>
      {% ctx.component.values.forEach(function(value) { %}
      <th style="text-align: center;">
        {{ctx.t(value.label, { _userInput: true })}}
        {% if (value.tooltip) { %}
          <i ref="tooltip" class="{{ctx.iconClass('question-sign')}} text-muted-foreground" data-tooltip="{{value.tooltip}}"></i>
        {% } %}
      </th>
      {% }) %}
    </tr>
  </thead>
  <tbody>
    {% ctx.component.questions.forEach(function(question) { %}
    <tr>
      <td>
        {{ctx.t(question.label)}}
        {% if (question.tooltip) { %}
          <i ref="tooltip" class="{{ctx.iconClass('question-sign')}} text-muted-foreground" data-tooltip="{{question.tooltip}}"></i>
        {% } %}
      </td>
      {% ctx.component.values.forEach(function(value) { %}
      <td style="text-align: center;">
        <input type="radio" name="{{ ctx.self.getInputName(question) }}" value="{{value.value}}" id="{{ctx.key}}-{{question.value}}-{{value.value}}" ref="input">
      </td>
      {% }) %}
    </tr>
    {% }) %}
  </tbody>
</table>
